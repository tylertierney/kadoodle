{
  "name": "@kadoodle/backend",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "continuous": true,
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@kadoodle/backend:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@kadoodle/backend:build:development"
          },
          "production": {
            "buildTarget": "@kadoodle/backend:build:production"
          }
        }
      }
    }
  }
}
